/* ==========================================================================
   JOLLIEBEAR DESIGN SYSTEM & GLOBAL STYLES
   ========================================================================== */

:root {
    /* Color Palette */
    --primary: hsl(354, 85%, 48%);       /* Jolliebear Crimson Red */
    --primary-hover: hsl(354, 85%, 42%);
    --primary-light: hsl(354, 85%, 95%);
    --secondary: hsl(42, 100%, 53%);     /* Golden Honey Yellow */
    --secondary-hover: hsl(42, 100%, 48%);
    --accent: hsl(14, 91%, 55%);         /* Spicy Orange */
    --text-dark: hsl(210, 24%, 16%);     /* Charcoal Slate */
    --text-muted: hsl(210, 12%, 46%);    /* Muted Slate */
    --bg-light: hsl(34, 40%, 96%);       /* Warm Light Beige */
    --bg-white: #ffffff;
    --border-color: hsl(210, 14%, 90%);
    
    /* Gradients */
    --grad-red: linear-gradient(135deg, hsl(354, 85%, 48%) 0%, hsl(354, 90%, 60%) 100%);
    --grad-yellow: linear-gradient(135deg, hsl(42, 100%, 50%) 0%, hsl(42, 100%, 65%) 100%);
    --grad-dark: linear-gradient(135deg, hsl(210, 24%, 16%) 0%, hsl(210, 24%, 26%) 100%);

    /* Fonts */
    --font-primary: 'Outfit', 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    
    /* Layout Properties */
    --header-height: 80px;
    --utility-height: 40px;
    --border-radius-sm: 8px;
    --border-radius-md: 16px;
    --border-radius-lg: 32px;
    --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.06);
    --shadow-md: 0 8px 24px rgba(0, 0, 0, 0.08);
    --shadow-lg: 0 16px 40px rgba(217, 29, 44, 0.15);
    
    /* Transitions */
    --transition-fast: 0.2s ease;
    --transition-smooth: 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
}

/* Reset & Base Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
    font-size: 16px;
}

body {
    font-family: var(--font-primary);
    background-color: var(--bg-light);
    color: var(--text-dark);
    line-height: 1.5;
    overflow-x: hidden;
}

a {
    text-decoration: none;
    color: inherit;
    transition: var(--transition-fast);
}

ul {
    list-style: none;
}

img {
    max-width: 100%;
    height: auto;
    display: block;
}

button {
    font-family: var(--font-primary);
    border: none;
    background: none;
    cursor: pointer;
    transition: var(--transition-fast);
}

input {
    font-family: var(--font-primary);
    border: none;
    outline: none;
}

/* Common Components */
.btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 12px 28px;
    border-radius: var(--border-radius-lg);
    font-weight: 600;
    font-size: 1rem;
    transition: var(--transition-smooth);
}

.btn-primary {
    background-color: var(--secondary);
    color: var(--text-dark);
    box-shadow: 0 4px 12px rgba(255, 178, 0, 0.3);
}

.btn-primary:hover {
    background-color: var(--secondary-hover);
    transform: translateY(-2px);
    box-shadow: 0 6px 16px rgba(255, 178, 0, 0.4);
}

/* Animations */
@keyframes float {
    0% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-12px) rotate(5deg); }
    100% { transform: translateY(0px) rotate(0deg); }
}

.animate-float {
    animation: float 4s ease-in-out infinite;
}

/* ==========================================================================
   1. TOP UTILITY BAR
   ========================================================================== */

.utility-bar {
    height: var(--utility-height);
    background-color: var(--primary-hover);
    color: var(--bg-white);
    font-size: 0.85rem;
    font-weight: 500;
    border-bottom: 2px solid var(--secondary);
    position: relative;
    z-index: 101;
}

.utility-container {
    max-width: 1200px;
    margin: 0 auto;
    height: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 20px;
}

.utility-right {
    display: flex;
    align-items: center;
    gap: 24px;
    height: 100%;
}

/* Language Selector */
.language-selector {
    display: flex;
    align-items: center;
    background-color: rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    padding: 2px;
}

.lang-item {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 4px 12px;
    border-radius: 16px;
    cursor: pointer;
    transition: var(--transition-fast);
}

.lang-item.active {
    background-color: var(--secondary);
    color: var(--text-dark);
    font-weight: 700;
}

.flag-icon {
    width: 18px;
    height: 12px;
    display: inline-block;
    border-radius: 2px;
    background-size: cover;
    background-position: center;
}

.vi-flag {
    background-image: linear-gradient(to bottom, #da251d 50%, #da251d 50%), 
                      radial-gradient(circle, #ffff00 35%, transparent 40%);
    position: relative;
}

.vi-flag::before {
    content: '★';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -52%);
    color: #ffff00;
    font-size: 11px;
}

.en-flag {
    background: linear-gradient(90deg, #00247d 30%, transparent 30%),
                linear-gradient(#00247d 40%, transparent 40%);
    background-color: #cf142b;
    position: relative;
}

.en-flag::before {
    content: '+';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -60%);
    color: #ffffff;
    font-size: 14px;
    font-weight: bold;
}

/* Auth Button */
.auth-btn {
    display: flex;
    align-items: center;
    gap: 8px;
    color: var(--bg-white);
    font-weight: 600;
    height: 100%;
    padding: 0 10px;
}

.auth-btn:hover {
    color: var(--secondary);
}

/* ==========================================================================
   2. MAIN HEADER & NAVBAR
   ========================================================================== */

.main-header {
    height: var(--header-height);
    background-color: var(--primary);
    box-shadow: var(--shadow-sm);
    position: sticky;
    top: 0;
    z-index: 100;
    transition: var(--transition-smooth);
}

.main-header.scrolled {
    height: 70px;
    box-shadow: var(--shadow-md);
}

.header-container {
    max-width: 1200px;
    margin: 0 auto;
    height: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 20px;
}

/* Logo Area */
.logo-area {
    display: flex;
    align-items: center;
    gap: 12px;
}

.logo-circle {
    width: 48px;
    height: 48px;
    background-color: var(--secondary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
    border: 2px solid var(--bg-white);
    transition: var(--transition-smooth);
}

.logo-area:hover .logo-circle {
    transform: scale(1.08) rotate(-10deg);
}

.logo-icon {
    font-size: 1.5rem;
    color: var(--primary);
}

.logo-text {
    display: flex;
    flex-direction: column;
}

.logo-brand {
    font-size: 1.6rem;
    font-weight: 800;
    color: var(--bg-white);
    letter-spacing: -0.5px;
    line-height: 1.1;
}

.logo-slogan {
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--secondary);
    letter-spacing: 0.5px;
}

/* Navbar */
.navbar {
    height: 100%;
}

.nav-menu {
    display: flex;
    align-items: center;
    height: 100%;
    gap: 4px;
}

.nav-item {
    height: 100%;
    display: flex;
    align-items: center;
}

.nav-link {
    color: var(--bg-white);
    font-weight: 700;
    font-size: 0.85rem;
    padding: 8px 14px;
    border-radius: 20px;
    letter-spacing: 0.2px;
    transition: var(--transition-smooth);
}

.nav-link:hover {
    color: var(--secondary);
}

.nav-link.active {
    background-color: var(--bg-white);
    color: var(--primary);
    box-shadow: var(--shadow-sm);
}

/* Mobile Menu Toggle */
.mobile-nav-toggle {
    display: none;
    font-size: 1.5rem;
    color: var(--bg-white);
    padding: 8px;
}

/* ==========================================================================
   3. HERO CAROUSEL
   ========================================================================== */

.hero-carousel {
    position: relative;
    width: 100%;
    height: 480px;
    overflow: hidden;
}

.carousel-track-container {
    height: 100%;
    width: 100%;
}

.carousel-track {
    position: relative;
    height: 100%;
    width: 100%;
}

.carousel-slide {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 100%;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.8s ease, visibility 0.8s ease;
    z-index: 1;
}

.carousel-slide.current-slide {
    opacity: 1;
    visibility: visible;
    z-index: 2;
}

/* Slide Gradients & Backgrounds */
.bg-gradient-1 {
    background: linear-gradient(135deg, hsl(32, 98%, 51%) 0%, hsl(43, 100%, 55%) 100%);
}

.bg-gradient-2 {
    background: linear-gradient(135deg, hsl(354, 85%, 48%) 0%, hsl(354, 80%, 58%) 100%);
}

.bg-gradient-3 {
    background: linear-gradient(135deg, hsl(340, 85%, 35%) 0%, hsl(14, 85%, 48%) 100%);
}

.slide-content {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
}

.slide-inner {
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
    padding: 0 40px;
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    align-items: center;
    gap: 40px;
}

/* Slide Text Elements */
.slide-text {
    color: var(--bg-white);
}

.slide-text .badge {
    background-color: var(--primary);
    color: var(--bg-white);
    padding: 6px 14px;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 1px;
    display: inline-block;
    margin-bottom: 16px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

.bg-gradient-2 .slide-text .badge {
    background-color: var(--secondary);
    color: var(--text-dark);
}

.slide-text h2 {
    font-size: 3.5rem;
    font-weight: 800;
    line-height: 1.15;
    margin-bottom: 16px;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.15);
}

.slide-text p {
    font-size: 1.1rem;
    margin-bottom: 24px;
    opacity: 0.95;
    max-width: 540px;
    font-weight: 300;
}

.price-tag {
    display: flex;
    align-items: baseline;
    gap: 10px;
    margin-bottom: 28px;
}

.price-tag .price {
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--bg-white);
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
}

.price-tag .old-price {
    font-size: 1.3rem;
    text-decoration: line-through;
    opacity: 0.65;
}

.price-tag .unit {
    font-size: 1.2rem;
    opacity: 0.85;
}

/* Slide Graphic Elements */
.slide-graphic {
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
}

.food-plate {
    width: 280px;
    height: 280px;
    background-color: rgba(255, 255, 255, 0.9);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.2), inset 0 0 20px rgba(0, 0, 0, 0.05);
    position: relative;
    border: 6px solid rgba(255, 255, 255, 0.5);
}

.graphic-shadow {
    position: absolute;
    bottom: -30px;
    width: 85%;
    height: 20px;
    background: radial-gradient(ellipse at center, rgba(0,0,0,0.25) 0%, rgba(0,0,0,0) 70%);
    border-radius: 50%;
}

.graphic-food-icon {
    font-size: 7.5rem;
    color: var(--primary);
    filter: drop-shadow(0 8px 15px rgba(0, 0, 0, 0.15));
}

.bg-gradient-2 .graphic-food-icon {
    color: var(--accent);
}

.bg-gradient-3 .graphic-food-icon {
    color: var(--primary);
}

/* Carousel Indicators */
.carousel-nav {
    position: absolute;
    bottom: 24px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 12px;
    z-index: 10;
}

.carousel-indicator {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background-color: rgba(255, 255, 255, 0.4);
    border: 2px solid transparent;
    transition: var(--transition-smooth);
}

.carousel-indicator:hover {
    background-color: rgba(255, 255, 255, 0.8);
}

.carousel-indicator.current-slide-indicator {
    background-color: var(--primary);
    border-color: var(--bg-white);
    transform: scale(1.2);
}

/* ==========================================================================
   4. ORDER SELECTOR & SEARCH BAR
   ========================================================================== */

.order-section {
    position: relative;
    margin-top: -40px;
    z-index: 50;
    padding: 0 20px;
}

.order-container {
    max-width: 800px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
}

/* Toggle Pill Wrapper */
.order-toggle-wrapper {
    display: inline-flex;
    background-color: #e2e8f0;
    padding: 6px;
    border-radius: 40px;
    box-shadow: var(--shadow-sm);
}

.toggle-btn {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 28px;
    border-radius: 30px;
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--text-dark);
    transition: var(--transition-smooth);
}

.toggle-btn.active {
    background-color: var(--primary);
    color: var(--bg-white);
    box-shadow: 0 4px 12px rgba(217, 29, 44, 0.25);
}

.toggle-btn:not(.active):hover {
    background-color: rgba(0, 0, 0, 0.05);
}

/* Address Search Box */
.address-search-box {
    width: 100%;
    background-color: var(--bg-white);
    border-radius: 40px;
    padding: 8px;
    box-shadow: var(--shadow-md);
    border: 1px solid var(--border-color);
    transition: var(--transition-smooth);
}

.address-search-box:focus-within {
    box-shadow: var(--shadow-lg);
    border-color: var(--primary);
}

.input-wrapper {
    display: flex;
    align-items: center;
    width: 100%;
    padding: 0 8px 0 16px;
}

.input-loc-icon {
    font-size: 1.25rem;
    color: var(--primary);
    margin-right: 12px;
}

#address-input {
    flex: 1;
    font-size: 1.05rem;
    color: var(--text-dark);
    background: transparent;
    padding: 10px 0;
}

#address-input::placeholder {
    color: var(--text-muted);
    font-weight: 400;
}

.search-btn {
    width: 48px;
    height: 48px;
    background-color: var(--primary);
    color: var(--bg-white);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.15rem;
    box-shadow: 0 4px 10px rgba(217, 29, 44, 0.2);
}

.search-btn:hover {
    background-color: var(--primary-hover);
    transform: scale(1.05);
}

/* ==========================================================================
   5. "ĂN GÌ HÔM NAY" FEATURED SECTION
   ========================================================================== */

.featured-section {
    max-width: 1200px;
    margin: 80px auto;
    padding: 0 20px;
}

.featured-container {
    display: grid;
    grid-template-columns: 1fr 4fr;
    gap: 20px;
    align-items: stretch;
}

/* Left Brand Promo Card */
.promo-brand-card {
    background: var(--grad-red);
    border-radius: var(--border-radius-md);
    color: var(--bg-white);
    padding: 40px 30px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
    overflow: hidden;
    box-shadow: var(--shadow-md);
}

.promo-brand-card::before {
    content: '';
    position: absolute;
    top: -50px;
    right: -50px;
    width: 180px;
    height: 180px;
    background-color: rgba(255, 255, 255, 0.05);
    border-radius: 50%;
}

.promo-content {
    position: relative;
    z-index: 2;
    text-align: center;
}

.brand-mascot-wrapper {
    width: 80px;
    height: 80px;
    background-color: var(--secondary);
    border-radius: 50%;
    margin: 0 auto 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-sm);
    border: 3px solid var(--bg-white);
    animation: float 5s ease-in-out infinite;
}

.mascot-icon {
    font-size: 2.2rem;
    color: var(--primary);
}

.promo-brand-card h2 {
    font-size: 2.2rem;
    font-weight: 800;
    line-height: 1.2;
    margin-bottom: 16px;
    letter-spacing: -0.5px;
    text-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
}

.promo-brand-card p {
    font-size: 0.95rem;
    opacity: 0.9;
    line-height: 1.6;
    font-weight: 300;
}

/* Right Food Category Grid */
.category-cards-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
}

/* Wood Textured Card Layout */
.wood-card {
    /* Custom CSS Wood Texture Background */
    background-color: #f7f3ed;
    background-image: 
        linear-gradient(90deg, rgba(215, 206, 192, 0.25) 0%, transparent 1%),
        linear-gradient(90deg, rgba(168, 153, 131, 0.15) 1%, transparent 3%),
        linear-gradient(90deg, rgba(255, 255, 255, 0.4) 0%, rgba(255, 255, 255, 0) 100%),
        repeating-linear-gradient(90deg, transparent, transparent 40px, rgba(150, 130, 100, 0.04) 40px, rgba(150, 130, 100, 0.04) 80px);
    border: 1px solid rgba(168, 153, 131, 0.3);
    border-radius: var(--border-radius-md);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 30px 20px 24px;
    box-shadow: var(--shadow-sm);
    transition: var(--transition-smooth);
    position: relative;
}

.wood-card::after {
    /* Subtle gloss overlay */
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 45%;
    background: linear-gradient(to bottom, rgba(255, 255, 255, 0.4) 0%, rgba(255, 255, 255, 0) 100%);
    pointer-events: none;
}

.wood-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-md);
    border-color: rgba(217, 29, 44, 0.3);
}

.card-image-wrapper {
    margin-bottom: 24px;
    position: relative;
    z-index: 2;
}

.food-mock-img {
    width: 140px;
    height: 140px;
    background-color: var(--bg-white);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
    border: 4px solid rgba(255, 255, 255, 0.8);
    transition: var(--transition-smooth);
}

.wood-card:hover .food-mock-img {
    transform: scale(1.05) rotate(5deg);
    box-shadow: 0 14px 30px rgba(0, 0, 0, 0.12);
}

.food-mock-icon {
    font-size: 3.8rem;
}

.chicken-mock .food-mock-icon { color: hsl(32, 98%, 51%); }
.spicy-mock .food-mock-icon { color: var(--primary); }
.spaghetti-mock .food-mock-icon { color: var(--accent); }
.dessert-mock .food-mock-icon { color: hsl(200, 75%, 60%); }

.card-info {
    flex: 1;
    display: flex;
    flex-direction: column;
    position: relative;
    z-index: 2;
}

.card-info h3 {
    font-size: 1.15rem;
    font-weight: 800;
    color: var(--text-dark);
    margin-bottom: 8px;
    line-height: 1.3;
}

.card-info p {
    font-size: 0.85rem;
    color: var(--text-muted);
    margin-bottom: 20px;
    line-height: 1.5;
    flex: 1;
}

.card-link {
    font-size: 0.9rem;
    font-weight: 700;
    color: var(--primary);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 4px;
}

.card-link:hover {
    color: var(--primary-hover);
    gap: 8px;
}

/* ==========================================================================
   6. FOOTER SECTION
   ========================================================================== */

.site-footer {
    background: var(--grad-dark);
    color: rgba(255, 255, 255, 0.8);
    padding: 80px 0 40px;
    border-top: 5px solid var(--secondary);
}

.footer-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

.footer-top {
    display: grid;
    grid-template-columns: 1.5fr 1fr 1fr 1.2fr;
    gap: 40px;
    margin-bottom: 60px;
}

.footer-brand {
    display: flex;
    flex-direction: column;
    gap: 16px;
}

.footer-logo {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 1.8rem;
    font-weight: 800;
    color: var(--bg-white);
}

.footer-logo i {
    color: var(--secondary);
}

.footer-desc {
    font-size: 0.9rem;
    line-height: 1.6;
    opacity: 0.7;
}

.social-links {
    display: flex;
    gap: 12px;
    margin-top: 8px;
}

.social-links a {
    width: 38px;
    height: 38px;
    background-color: rgba(255, 255, 255, 0.08);
    color: var(--bg-white);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.95rem;
}

.social-links a:hover {
    background-color: var(--secondary);
    color: var(--text-dark);
    transform: translateY(-3px);
}

.footer-links-group h4 {
    color: var(--bg-white);
    font-size: 1.05rem;
    font-weight: 700;
    margin-bottom: 24px;
    position: relative;
    letter-spacing: 0.5px;
}

.footer-links-group h4::after {
    content: '';
    position: absolute;
    bottom: -8px;
    left: 0;
    width: 35px;
    height: 2px;
    background-color: var(--secondary);
}

.footer-links-group ul {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.footer-links-group ul li {
    font-size: 0.9rem;
}

.footer-links-group ul li a:hover {
    color: var(--secondary);
    padding-left: 4px;
}

.footer-links-group ul li i {
    color: var(--secondary);
    margin-right: 8px;
    width: 16px;
    text-align: center;
}

.footer-bottom {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding-top: 30px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.85rem;
    opacity: 0.7;
    flex-wrap: wrap;
    gap: 20px;
}

.footer-bottom-links {
    display: flex;
    gap: 24px;
}

.footer-bottom-links a:hover {
    color: var(--secondary);
}

/* ==========================================================================
   7. COOKIE CONSENT BANNER
   ========================================================================== */

.cookie-consent-banner {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background-color: hsl(210, 24%, 12%);
    color: var(--bg-white);
    padding: 16px 20px;
    box-shadow: 0 -10px 30px rgba(0, 0, 0, 0.3);
    z-index: 1000;
    border-top: 3px solid var(--primary);
    transform: translateY(100%);
    transition: transform 0.5s cubic-bezier(0.25, 0.8, 0.25, 1);
}

.cookie-consent-banner.show {
    transform: translateY(0);
}

.cookie-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 24px;
}

.cookie-text {
    font-size: 0.8rem;
    line-height: 1.6;
    flex: 1;
    opacity: 0.9;
}

.cookie-policy-link {
    color: var(--secondary);
    text-decoration: underline;
    font-weight: 500;
}

.cookie-actions {
    display: flex;
    gap: 12px;
    flex-shrink: 0;
}

.cookie-btn {
    padding: 8px 18px;
    font-size: 0.75rem;
    font-weight: 700;
    border-radius: 4px;
    text-transform: uppercase;
    transition: var(--transition-fast);
}

.btn-settings {
    background-color: transparent;
    color: var(--bg-white);
    border: 1px solid rgba(255, 255, 255, 0.3);
}

.btn-settings:hover {
    background-color: rgba(255, 255, 255, 0.08);
    border-color: var(--bg-white);
}

.btn-accept {
    background-color: var(--primary);
    color: var(--bg-white);
}

.btn-accept:hover {
    background-color: var(--primary-hover);
}

.btn-decline {
    background-color: var(--bg-white);
    color: var(--text-dark);
}

.btn-decline:hover {
    background-color: #e2e8f0;
}

/* ==========================================================================
   8. RESPONSIVE BREAKPOINTS
   ========================================================================== */

/* Tablet Layout (max-width: 1024px) */
@media (max-width: 1024px) {
    .featured-container {
        grid-template-columns: 1fr;
    }
    
    .promo-brand-card {
        padding: 40px;
        text-align: center;
    }
    
    .promo-brand-card h2 {
        font-size: 2rem;
    }
    
    .category-cards-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 20px;
    }
    
    .footer-top {
        grid-template-columns: repeat(2, 1fr);
    }
}

/* Mobile Navigation (max-width: 900px) */
@media (max-width: 900px) {
    .mobile-nav-toggle {
        display: block;
        z-index: 110;
    }
    
    .navbar {
        position: fixed;
        top: 0;
        right: -100%;
        width: 280px;
        height: 100vh;
        background-color: var(--primary-hover);
        box-shadow: -10px 0 30px rgba(0, 0, 0, 0.25);
        transition: var(--transition-smooth);
        padding: 100px 30px 40px;
        z-index: 105;
    }
    
    .navbar.open {
        right: 0;
    }
    
    .nav-menu {
        flex-direction: column;
        align-items: flex-start;
        height: auto;
        gap: 20px;
    }
    
    .nav-item {
        width: 100%;
    }
    
    .nav-link {
        font-size: 1.1rem;
        width: 100%;
        display: block;
        padding: 10px 20px;
    }
    
    .nav-link.active {
        background-color: var(--secondary);
        color: var(--text-dark);
    }
    
    /* Hero Carousel on Mobile */
    .hero-carousel {
        height: auto;
        min-height: 420px;
        padding: 40px 0;
    }
    
    .slide-inner {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 30px;
        padding: 0 20px;
    }
    
    .slide-text h2 {
        font-size: 2.2rem;
    }
    
    .slide-text p {
        font-size: 0.95rem;
        margin: 0 auto 20px;
    }
    
    .price-tag {
        justify-content: center;
        margin-bottom: 20px;
    }
    
    .slide-graphic {
        display: none; /* Hide food graphic for cleaner mobile presentation */
    }
}

/* Small Mobile Layout (max-width: 600px) */
@media (max-width: 600px) {
    .utility-bar {
        display: none; /* Hide top bar for extra space on mobile */
    }
    
    .main-header {
        height: 70px;
    }
    
    .logo-brand {
        font-size: 1.3rem;
    }
    
    .order-section {
        margin-top: -20px;
    }
    
    .toggle-btn {
        padding: 10px 16px;
        font-size: 0.8rem;
    }
    
    .address-search-box {
        border-radius: 30px;
    }
    
    .input-wrapper {
        padding: 0 4px 0 10px;
    }
    
    #address-input {
        font-size: 0.9rem;
    }
    
    .search-btn {
        width: 38px;
        height: 38px;
        font-size: 0.95rem;
    }
    
    .category-cards-grid {
        grid-template-columns: 1fr;
    }
    
    .cookie-container {
        flex-direction: column;
        align-items: stretch;
        gap: 16px;
        text-align: center;
    }
    
    .cookie-actions {
        flex-direction: column;
        width: 100%;
    }
    
    .cookie-btn {
        width: 100%;
    }
    
    .footer-top {
        grid-template-columns: 1fr;
        gap: 30px;
    }
    
    .footer-bottom {
        flex-direction: column;
        text-align: center;
    }
    
    .footer-bottom-links {
        justify-content: center;
        width: 100%;
    }
}
